<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Musium.Controls.QueueListItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Musium.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="6" Height="60" HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Spacing="12" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <Grid Height="40" CornerRadius="4">
                <Image Source="{x:Bind DisplayedCoverArt, Mode=OneWay, TargetNullValue='/Assets/Placeholder.png'}"  HorizontalAlignment="Left"/>
            </Grid>
            <StackPanel>
                <TextBlock Text="{x:Bind Song.Title, Mode=OneWay, TargetNullValue='No Song'}" FontWeight="Medium"/>
                <TextBlock Text="{x:Bind Song.Album.Artist.Name, Mode=OneWay, TargetNullValue='No Artist'}" FontSize="12" FontWeight="Light"/>
            </StackPanel>
        </StackPanel>
        <Grid Grid.Column="1" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Margin="16,0">
            <SymbolIcon Symbol="GlobalNavigationButton"/>
        </Grid>
    </Grid>
</UserControl>
