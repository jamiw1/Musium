<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="localMusicPlayerTest.Pages.NowPlaying"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:localMusicPlayerTest.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:localMusicPlayerTest.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="PlaybackButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="CornerRadius" Value="50"/>
        </Style>
    </Page.Resources>

    <Grid Padding="16" ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
            <Frame CornerRadius="4">
                <Image Source="/Assets/Placeholder.png" MaxWidth="320"/>
            </Frame>
            <StackPanel Spacing="4">
                <TextBlock Text="Example Song Title" HorizontalAlignment="Center" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock Text="Example Song Artist" HorizontalAlignment="Center" FontSize="16" FontWeight="Normal"/>
            </StackPanel>
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="0:30" FontSize="14" FontWeight="Light" VerticalAlignment="Center"/>
                <Slider x:Name="ProgressSlider"
                    Grid.Column="1"
                    Minimum="0" 
                    Maximum="123"
                    TickFrequency="1"
                    TickPlacement="None"
                    Margin="16,0"
                    IsThumbToolTipEnabled="False"/>
                <TextBlock Grid.Column="2" Text="2:03" FontSize="14" FontWeight="Light" VerticalAlignment="Center"/>
            </Grid>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="16">
                <Button Style="{StaticResource PlaybackButtonStyle}">
                    <FontIcon Glyph="&#xeb9e;"/>
                </Button>
                <Button Style="{StaticResource PlaybackButtonStyle}" Click="PlayButton_Click">
                    <SymbolIcon Symbol="Play"/>
                </Button>
                <Button Style="{StaticResource PlaybackButtonStyle}">
                    <FontIcon Glyph="&#xeb9d;"/>
                </Button>
            </StackPanel>
        </StackPanel>
        <Grid Grid.Column="1" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Queue" FontSize="20" FontWeight="SemiBold" Grid.Row="0"/>
            <Grid
                CornerRadius="4" 
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                Grid.Row="1">
                <ScrollViewer  
                    HorizontalScrollBarVisibility="Auto"
                    Padding="8">
                    <StackPanel VerticalAlignment="Top">
                        <controls:QueueListItemControl />
                        <controls:QueueListItemControl />
                        <controls:QueueListItemControl />
                        <controls:QueueListItemControl />
                        <controls:QueueListItemControl />
                        <controls:QueueListItemControl />
                        <controls:QueueListItemControl />
                        <controls:QueueListItemControl />
                        <controls:QueueListItemControl />
                        <controls:QueueListItemControl />
                        <controls:QueueListItemControl />
                        <controls:QueueListItemControl />
                        <controls:QueueListItemControl />
                        <controls:QueueListItemControl />
                        <controls:QueueListItemControl />
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Page>
