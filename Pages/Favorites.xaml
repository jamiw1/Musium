<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Musium.Pages.Favorites"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Musium.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Musium.Controls"
    xmlns:models="using:Musium.Models"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="NakedListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <ListView Grid.Row="1"
            ItemsSource="{x:Bind AllFavoriteTracks}"
            IsItemClickEnabled="False" 
            ItemContainerStyle="{StaticResource NakedListViewItemStyle}"
            Padding="16">
            <ListView.Header>
                <StackPanel Orientation="Horizontal" Margin="0,12,0,4" Spacing="32" VerticalAlignment="Center">
                    <TextBlock Text="Favorites" Style="{ThemeResource SubtitleTextBlockStyle}" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Click="PlayButton_Click" Style="{StaticResource AccentButtonStyle}">
                            <StackPanel Orientation="Horizontal" Spacing="8" Padding="2">
                                <SymbolIcon Symbol="Play"/>
                                <TextBlock Text="Play"/>
                            </StackPanel>
                        </Button>
                        <Button Click="ShuffleButton_Click">
                            <StackPanel Orientation="Horizontal" Spacing="8" Padding="2">
                                <SymbolIcon Symbol="Shuffle"/>
                                <TextBlock Text="Shuffle"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Song">
                    <controls:TrackItemControl 
                        Song="{x:Bind}"
                        TrackView="True"
                        Clicked="TrackItemControl_Clicked"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
